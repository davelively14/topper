<h2>New intersection</h2>

<%= render "form.html", changeset: @changeset,
                        action: intersection_path(@conn, :create),
                        street_sel: @street_sel %>

<%= link "Back", to: intersection_path(@conn, :index) %>
