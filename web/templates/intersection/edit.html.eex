<h2>Edit intersection</h2>

<%= render "form.html", changeset: @changeset,
                        action: intersection_path(@conn, :update, @intersection) %>

<%= link "Back", to: intersection_path(@conn, :index) %>
